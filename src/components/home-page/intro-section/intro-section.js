import React from 'react';

export default function IntroSection() {
    return (
        <section>
            <h2>MoultDB</h2>
            <p>MoultDB is is a resource on moulting in arthropods (from paleontology to genomics).</p>
        </section>
    );
}